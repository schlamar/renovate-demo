[project]
name = "renovate-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "schlaich", email = "marc.schlaich@hermle.de" }
]
requires-python = ">=3.13"
dependencies = [
    "uvicorn==0.42.0",
]

[project.scripts]
renovate-demo = "renovate_demo:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
